services:
  font-cdn:
    image: nginx:alpine
    container_name: font-cdn
    ports:
      - "8080:80"
    volumes:
      - ./fonts:/usr/share/nginx/html/fonts:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
