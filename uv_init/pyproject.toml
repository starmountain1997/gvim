[project]
name = "uv-init-dev"
version = "0.1.0"
description = "Initialize uv project with development dependencies and Claude config"
dependencies = ["typer>=0.12.0"]

[project.scripts]
uv-init-dev = "uv_init_dev.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
